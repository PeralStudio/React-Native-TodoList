import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginBottom: 100,
    },
    textButton: {
        color: "#fff",
        textTransform: "uppercase",
    },
    text: {
        color: "white",
        fontSize: 20,
        textAlign: "center",
        marginBottom: 30,
    },
    icon: {
        marginBottom: 10,
        textAlign: "center",
    },
});
